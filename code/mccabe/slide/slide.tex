% C++ London: McCabe

\input{../../../presentation/preamble.tex}

\begin{document}

\begin{frame}[fragile]{Cyclomatic Complexity}
\begin{columns}
  \begin{column}{0.5\textwidth}
    \begin{center}
    \begin{tikzpicture}[thick]
      \tikzset{bb/.style={draw, circle, inner sep=0.3cm}}

      \path (0, 0) coordinate [bb] (1);
      \path (-1, -2) coordinate [bb] (2);
      \path (+1, -2) coordinate [bb] (3);
      \path (0, -4) coordinate [bb] (4);

      \foreach \i/\j in {1/2, 2/4, 1/3, 3/4} {
        \draw [->] (\i) -- (\j);
      }

    \end{tikzpicture}
  \end{center}
  \end{column}
  \begin{column}{0.5\textwidth}
    \begin{center}
    \begin{lstlisting}
      int x = f();

      if (x > 5) {
        x += 1;
      } else {
        x -= 1;
      }

      return x;
    \end{lstlisting}
  \end{center}
  \end{column}
\end{columns}
\end{frame}

\begin{frame}[fragile]{Cyclomatic Complexity}
\begin{columns}
  \begin{column}{0.5\textwidth}
    \begin{center}
    \begin{tikzpicture}[thick]
      \tikzset{bb/.style={draw, circle, inner sep=0.3cm}}

      \path (0, 0) coordinate [bb] (1);
      \path (-1, -2) coordinate [bb] (2);
      \path (+1, -2) coordinate [bb] (3);
      \path (+1.5, -3.3) coordinate [bb, inner sep=0.25cm] (4);
      \path (0, -4) coordinate [bb] (5);

      \foreach \i/\j in {1/2, 2/5, 1/3, 3/4, 4/5, 3/5} {
        \draw [->] (\i) -- (\j);
      }

    \end{tikzpicture}
  \end{center}
  \end{column}
  \begin{column}{0.5\textwidth}
    \begin{center}
    \begin{lstlisting}
      int x = f();

      if (x > 5) {
        x += 1;
        if (x < 10) {
          x += 123;
        }
      } else {
        x -= 1;
      }

      return x;
    \end{lstlisting}
  \end{center}
  \end{column}
\end{columns}
\end{frame}

\end{document}
