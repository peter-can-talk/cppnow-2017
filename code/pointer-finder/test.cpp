#include <vector>
#include <string>

long * p;
class X { int* pointer; };
void f(short*) { };
int *q = new int[5];
std::vector<std::string::iterator>*
	vsip = new std::vector<std::string::iterator>();
int* p_pointer;
void (*fp) (int, int);
